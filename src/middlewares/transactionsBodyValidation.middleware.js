/* import { transactionsSchema } from "../models/transactionsSchema";

export async function teste(req, res, next){
    const { teste } = req.headers;

    if (!authorization){
        res.status(401).send("Headers inválido")
    }
    const validateTransactions = transactionsSchema.validate(req.body, {abortEarly: false});
} */